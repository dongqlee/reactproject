import { useState } from "react"
import prodata from "./Prodata"
export default function Men() {
  const prodata = useState(prodata)

  return (
    <>
    {
      prodata.map((prodata, i) => {
        return (
          
        )
      })
    }
      
    </>
  )
}